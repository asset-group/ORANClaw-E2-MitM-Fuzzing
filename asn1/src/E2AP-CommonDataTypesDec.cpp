/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.8.2.5, Date: 12-Sep-2025.
 *
 * Command:  asn1c asn1files/E2SM-KPM-v05.00.asn asn1files/asn_flexric/e2sm_rc_v1_03_modified.asn asn1files/asn_flexric/e42ap_v2_03.asn -genTest -per -json -w32 -server -depends -events -genPrtToStr -list -reader -trace -c++ -print -prtfmt details -srcdir src -make Makefile -w32 -table-unions -stream -pdu all
 */
#include "E2AP-CommonDataTypes.h"
#include "rtxsrc/rtxCharStr.h"
#include "rtxsrc/rtxDiag.h"

EXTERN int asn1JsonDec_ProcedureCode (OSCTXT *pctxt, ASN1T_ProcedureCode *pvalue)
{
   int stat = 0;

   stat = rtJsonDecUInt8Value (pctxt, pvalue);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }

   return stat;
}

EXTERN int asn1PD_ProcedureCode (OSCTXT* pctxt, ASN1T_ProcedureCode* pvalue)
{
   int stat = 0;

   RTXCTXTPUSHTYPENAME (pctxt, "ProcedureCode");

   RTDIAGSTRM2 (pctxt,"asn1PD_ProcedureCode: start\n");

   stat = PD_BYTE_ALIGN(pctxt);
   if (stat != 0) return LOG_RTERR (pctxt, stat);
   PU_NEWFIELD(pctxt, "constrained int");

   stat = rtxDecBitsToByte(pctxt, pvalue, 8);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   PU_SETBITCOUNT(pctxt);

   Asn1NamedEventHandler::invokeUIntValue (pctxt, *pvalue);

   RTDIAGSTRM2 (pctxt,"asn1PD_ProcedureCode: end\n");

   RTXCTXTPOPTYPENAME (pctxt);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }
   return (stat);
}

EXTERN int asn1JsonDec_Criticality (OSCTXT *pctxt, ASN1T_Criticality *pvalue)
{
   int stat = 0;

   {
      OSINT32 enumVarIdx_;
      OSUTF8CHAR* ltmpstr;
      stat = rtJsonDecStringValue (pctxt, &ltmpstr);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      enumVarIdx_ = rtxLookupEnum (ltmpstr, rtxUTF8LenBytes(ltmpstr), ASN1T_Criticality_ENUMTAB, ASN1T_Criticality_ENUMTABSIZE);

      if (enumVarIdx_ >= 0){
         *pvalue = (OSUINT8)ASN1T_Criticality_ENUMTAB[enumVarIdx_].value;
         stat = 0;
      }
      else stat = RTERR_INVENUM;
      rtxMemFreePtr(pctxt, ltmpstr);
      if ( stat < 0 ) return LOG_RTERR(pctxt, stat);
   }
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (*pvalue >= 3) {
      int lstat = Asn1ErrorHandler::invoke (pctxt, 0, RTERR_INVENUM);
      if (lstat != 0)  {
         return LOG_RTERR (pctxt, lstat);
      }
   }
   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }

   return stat;
}

EXTERN int asn1PD_Criticality (OSCTXT* pctxt, ASN1T_Criticality* pvalue)
{
   int stat = 0;

   RTXCTXTPUSHTYPENAME (pctxt, "Criticality");

   RTDIAGSTRM2 (pctxt,"asn1PD_Criticality: start\n");

   {
      OSUINT32 tmpval;
      stat = pd_ConsUnsigned (pctxt, &tmpval, 0, OSUINTCONST(2));
      if (stat != 0) return LOG_RTERR (pctxt, stat);
      *pvalue = (OSUINT8)tmpval;
   }

   if (*pvalue > 2) {
      rtxErrAddElemNameParm (pctxt);
      rtxErrAddUIntParm (pctxt, *pvalue);
      return LOG_RTERR (pctxt, RTERR_CONSVIO);
   }

   Asn1NamedEventHandler::invokeEnumValue (pctxt, *pvalue,
      ASN1T_Criticality_ToString(*pvalue));

   RTDIAGSTRM2 (pctxt,"asn1PD_Criticality: end\n");

   RTXCTXTPOPTYPENAME (pctxt);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }
   return (stat);
}

EXTERN int asn1JsonDec_ProtocolIE_ID (OSCTXT *pctxt, ASN1T_ProtocolIE_ID *pvalue)
{
   int stat = 0;

   stat = rtJsonDecUInt16Value (pctxt, pvalue);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }

   return stat;
}

EXTERN int asn1PD_ProtocolIE_ID (OSCTXT* pctxt, ASN1T_ProtocolIE_ID* pvalue)
{
   int stat = 0;

   RTXCTXTPUSHTYPENAME (pctxt, "ProtocolIE-ID");

   RTDIAGSTRM2 (pctxt,"asn1PD_ProtocolIE_ID: start\n");

   stat = PD_BYTE_ALIGN(pctxt);
   if (stat != 0) return LOG_RTERR (pctxt, stat);
   PU_NEWFIELD(pctxt, "constrained int");

   stat = rtxDecBitsToUInt16(pctxt, pvalue, 16);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   PU_SETBITCOUNT(pctxt);

   Asn1NamedEventHandler::invokeUIntValue (pctxt, *pvalue);

   RTDIAGSTRM2 (pctxt,"asn1PD_ProtocolIE_ID: end\n");

   RTXCTXTPOPTYPENAME (pctxt);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }
   return (stat);
}

EXTERN int asn1JsonDec_TriggeringMessage (OSCTXT *pctxt, ASN1T_TriggeringMessage *pvalue)
{
   int stat = 0;

   {
      OSINT32 enumVarIdx_;
      OSUTF8CHAR* ltmpstr;
      stat = rtJsonDecStringValue (pctxt, &ltmpstr);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      enumVarIdx_ = rtxLookupEnum (ltmpstr, rtxUTF8LenBytes(ltmpstr), ASN1T_TriggeringMessage_ENUMTAB, ASN1T_TriggeringMessage_ENUMTABSIZE);

      if (enumVarIdx_ >= 0){
         *pvalue = (OSUINT8)ASN1T_TriggeringMessage_ENUMTAB[enumVarIdx_].value;
         stat = 0;
      }
      else stat = RTERR_INVENUM;
      rtxMemFreePtr(pctxt, ltmpstr);
      if ( stat < 0 ) return LOG_RTERR(pctxt, stat);
   }
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (*pvalue >= 3) {
      int lstat = Asn1ErrorHandler::invoke (pctxt, 0, RTERR_INVENUM);
      if (lstat != 0)  {
         return LOG_RTERR (pctxt, lstat);
      }
   }
   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }

   return stat;
}

EXTERN int asn1PD_TriggeringMessage (OSCTXT* pctxt, ASN1T_TriggeringMessage* pvalue)
{
   int stat = 0;

   RTXCTXTPUSHTYPENAME (pctxt, "TriggeringMessage");

   RTDIAGSTRM2 (pctxt,"asn1PD_TriggeringMessage: start\n");

   {
      OSUINT32 tmpval;
      stat = pd_ConsUnsigned (pctxt, &tmpval, 0, OSUINTCONST(2));
      if (stat != 0) return LOG_RTERR (pctxt, stat);
      *pvalue = (OSUINT8)tmpval;
   }

   if (*pvalue > 2) {
      rtxErrAddElemNameParm (pctxt);
      rtxErrAddUIntParm (pctxt, *pvalue);
      return LOG_RTERR (pctxt, RTERR_CONSVIO);
   }

   Asn1NamedEventHandler::invokeEnumValue (pctxt, *pvalue,
      ASN1T_TriggeringMessage_ToString(*pvalue));

   RTDIAGSTRM2 (pctxt,"asn1PD_TriggeringMessage: end\n");

   RTXCTXTPOPTYPENAME (pctxt);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }
   return (stat);
}

EXTERN int asn1JsonDec_Presence (OSCTXT *pctxt, ASN1T_Presence *pvalue)
{
   int stat = 0;

   {
      OSINT32 enumVarIdx_;
      OSUTF8CHAR* ltmpstr;
      stat = rtJsonDecStringValue (pctxt, &ltmpstr);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      enumVarIdx_ = rtxLookupEnum (ltmpstr, rtxUTF8LenBytes(ltmpstr), ASN1T_Presence_ENUMTAB, ASN1T_Presence_ENUMTABSIZE);

      if (enumVarIdx_ >= 0){
         *pvalue = (OSUINT8)ASN1T_Presence_ENUMTAB[enumVarIdx_].value;
         stat = 0;
      }
      else stat = RTERR_INVENUM;
      rtxMemFreePtr(pctxt, ltmpstr);
      if ( stat < 0 ) return LOG_RTERR(pctxt, stat);
   }
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (*pvalue >= 3) {
      int lstat = Asn1ErrorHandler::invoke (pctxt, 0, RTERR_INVENUM);
      if (lstat != 0)  {
         return LOG_RTERR (pctxt, lstat);
      }
   }
   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }

   return stat;
}

EXTERN int asn1PD_Presence (OSCTXT* pctxt, ASN1T_Presence* pvalue)
{
   int stat = 0;

   RTXCTXTPUSHTYPENAME (pctxt, "Presence");

   RTDIAGSTRM2 (pctxt,"asn1PD_Presence: start\n");

   {
      OSUINT32 tmpval;
      stat = pd_ConsUnsigned (pctxt, &tmpval, 0, OSUINTCONST(2));
      if (stat != 0) return LOG_RTERR (pctxt, stat);
      *pvalue = (OSUINT8)tmpval;
   }

   if (*pvalue > 2) {
      rtxErrAddElemNameParm (pctxt);
      rtxErrAddUIntParm (pctxt, *pvalue);
      return LOG_RTERR (pctxt, RTERR_CONSVIO);
   }

   Asn1NamedEventHandler::invokeEnumValue (pctxt, *pvalue,
      ASN1T_Presence_ToString(*pvalue));

   RTDIAGSTRM2 (pctxt,"asn1PD_Presence: end\n");

   RTXCTXTPOPTYPENAME (pctxt);

   if (pctxt->level == 0) {
      if (stat == 0) {
         stat = rtxErrCheckNonFatal (pctxt);
      }
   }
   return (stat);
}

